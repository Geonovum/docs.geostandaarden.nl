{
  "name": "docs-geostandaarden",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate:index": "node scripts/generate-index.js",
    "start": "serve -s -l ${PORT:-4173} ."
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
