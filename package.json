{
  "name": "docs-geostandaarden",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate:index": "node scripts/generate-index.js",
    "generate:serve-config": "node scripts/generate-serve-config.js",
    "build": "npm run generate:index && npm run generate:serve-config",
    "start": "node -e \"const port = process.env.PORT || 8080; const serve = require('serve'); serve('.', { port });\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
  